// This is a comment - computer ignores it
// console.log() prints text to the terminal

console.log("Hello, Node.js!");

// Variables store data
const name = "John";      // const = constant (can't change)
let age = 25;             // let = can change later
var oldWay = "don't use"; // var = old way (avoid)

// Template strings with ${}
console.log(`Name: ${name}, Age: ${age}`);