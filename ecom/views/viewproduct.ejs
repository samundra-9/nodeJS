<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyShop</title>
    <style>
        /* Container */
        .product-list-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 24px;
            background: #fafafa;
        }
    
        /* Section Title */
        .section-title {
            text-align: center;
            font-size: 1.8rem;
            margin-bottom: 24px;
            color: #264653;
            font-weight: 600;
        }
    
        /* Product Card */
        .product-card {
            background: #fff;
            border: 1px solid #eee;
            border-radius: 10px;
            padding: 24px;
            text-align: center;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
        }
    
        /* Image */
        .product-img {
            width: 200px;
            height: auto;
            margin-bottom: 16px;
            border-radius: 6px;
        }
    
        /* Text */
        .product-title {
            font-size: 1.6rem;
            margin-bottom: 12px;
            color: #2a9d8f;
        }
    
        .product-price {
            font-size: 1.4rem;
            color: #e76f51;
            margin-bottom: 16px;
        }
    
        .product-desc {
            font-size: 1rem;
            color: #555;
            margin-bottom: 16px;
        }
    
        .product-qty {
            font-size: 1rem;
            color: #555;
            margin-bottom: 24px;
        }
    
        /* Actions */
        .product-actions .btn {
            display: inline-block;
            margin: 0 8px;
            padding: 10px 16px;
            font-size: 1rem;
            color: #fff;
            background-color: #264653;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.2s ease;
        }
    
        .product-actions .btn:hover {
            background-color: #2a9d8f;
        }
    
        .product-actions .btn-secondary {
            background-color: #e76f51;
        }
    
        .product-actions .btn-secondary:hover {
            background-color: #f4a261;
        }
        
    </style>
</head>
<body>
    <%- include('navbar')  %>
    <main>
        <section class="product-list-container">
            <h1 class="section-title">Product Details</h1>
            <div class="product-card">
                <img src="../uploads/<%= product.image %>" alt="<%= product.title %>" class="product-img"/>
                <h2 class="product-title"><%= product.title %></h2>
                <p class="product-price">$<%= product.price %></p>
                <p class="product-desc"><%= product.description %></p>
                <p class="product-qty">Available Quantity: <%= product.quantity %></p>
                <div class="product-actions">
                    <a href="/addtocart/<%= product._id %>/inc" class="btn">+</a>
                    <a href="/addtocart/<%= product._id %>/dec" class="btn">-</a>
                    <a href="/removefromcart/<%= product._id %>" class="btn btn-secondary">🗑 Delete</a>
                    <a href="/dashboard" class="btn btn-secondary">🔙 Back to Dashboard</a>
                </div>
            </div>
        </section>  
    </main>
    <%- include('footer')  %>
</body>
</html>